-- V2: Note — All 35 companies are already seeded in schema.sql (the master schema).
-- This migration file is intentionally left as a marker.
-- To re-seed companies independently, refer to schema.sql lines 160–440.
SELECT 1;

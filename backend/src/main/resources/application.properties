# Server Configuration
server.port=8080

# Database Configuration (MySQL using JDBC)
spring.datasource.url=jdbc:mysql://localhost:3306/careerforge_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Shri@2004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP Connection Pooling settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000

# JWT Configuration
jwt.secret=careerforgesecretkeyjwtplatformcareerguidanceplacementpreparationecosystem
jwt.expiration=86400000

# OpenAI Configuration (LinkedIn AI Profile Analysis)
# Leave openai.api.key blank to use the built-in rule-based analysis engine (no cost, works offline)
# To enable real GPT-powered analysis, set your OpenAI key below:
openai.api.key=
openai.model=gpt-4o-mini
